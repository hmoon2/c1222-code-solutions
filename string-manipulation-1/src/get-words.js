/* exported getWords */
/* create a function with one parameter (string)
 inside the function, create a new variable (wordsInString) have it equal  */

function getWords(string) {
  return string.split(' ');
}
