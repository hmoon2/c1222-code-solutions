/* exported getLastChar */
/* create a function with one argument
in the function code block, return the string at string.length - 1 */
function getLastChar(string) {
  return string[string.length - 1];
}
