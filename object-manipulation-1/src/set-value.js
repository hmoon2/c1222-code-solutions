/* exported setValue */
/* create a function with three parameters (object, key, value)
in the function assign value to the object at the key property */

function setValue(object, key, value) {
  object[key] = value;
}
