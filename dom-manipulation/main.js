/* First, declare three variables. One for the number of times a user clicks the button, a second for the querySelector
for the hot-button class, and a third for the querySelector for the click-count class. Create a function that will be
used in the addEventListener method. In the function, when the 'click' event is fired then the counter variable will increment.
Next, create an event listener for the button element. The arguments will be 'click' and the function that was created.
udpate the function and have it update textContent every time it is clicked. Also, in the function, make it so that as the
number of clicks go up the textContent changes.  */

// var numberOfClicks = 0;
// var clickHotButton = document.querySelector('.hot-button');
// var clickCount = document.querySelector('.click-count');

// function hotButton(event) {
//   numberOfClicks++;
// }

// clickHotButton.addEventListener('click', hotButton);
