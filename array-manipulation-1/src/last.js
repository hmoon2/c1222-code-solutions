/* exported last */
/* create a function with one parameter and in that function return the last item in the array
to get the last item, subtract one from the length of the array and put it into array[] */

function last(array) {
  return array[array.length - 1];
}
